/**
 * Input ports (Driving ports) - Use case interfaces exposed to the outside world.
 *
 * These interfaces define WHAT the application can do (its capabilities).
 * They are implemented by use cases in the application layer and
 * called by driving adapters (e.g., REST controllers).
 */
package com.upkeep.application.port.in;

